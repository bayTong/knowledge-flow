# 外置第二大脑 · 战略愿景

> **状态：Draft（战略愿景；2026-09-01）**：本文表达长期方向，不是当前实现说明。捕获第一原件、人工闸门和 MVP 顺序以[设计权威与冲突登记](design-authority-and-conflict-register-设计权威与冲突登记.md)为准；“人审 → 策展入库”必须等待新 SOP-002 的精确批准、事务和回滚协议。
>
> 本文档描述外置第二大脑项目的核心理念和架构总览。
> 长期阶段候选见 [`docs/build-plan.md`](build-plan.md)，当前执行顺序仍以设计权威登记为准。
>
> **历史关系说明**：多知识库路由、主动检索、MC-001 与 [`gbrain-integration-plan.md`](gbrain-integration-plan.md) 中的实现设想都是远期候选，彼此不再相互取代；采用前必须分别提案和验收。

---

## 什么是外置第二大脑

不是「AI 帮你记笔记」，而是 **AI 智能体作为持续运行的认知外挂**：

- **摄入管线**：AI 穷举提取 → 独立覆盖审计 → 人审 → 策展入库。覆盖审查与提取分离——飞行员和塔台是不同的人。
- **持久记忆**：知识库作为 AI 的跨会话长期记忆。AI 在对话中主动检索已有知识，而非从零开始。
- **元认知监控**：独立 Agent 定期扫描知识库健康度——遗漏、漂移、盲区、结构趋势。
- **自我进化**：系统从人的审核反馈中学习自己的盲区，持续改进提取和策展质量。

---

## 核心原则

| 原则 | 含义 |
|------|------|
| **AI 做覆盖，人做判断** | AI 穷举提取不遗漏；人决定什么重要、什么该入库 |
| **写入和检查分离** | 捕获组件只保存原件，提取 Agent 只生成提案，获批后由确定性执行器写可信知识；独立检查 Agent 只报告问题。避免上下文污染和越权写入 |
| **成本分层** | 最终产出用最好的模型；辅助检测用便宜模型；机械任务不用 LLM |
| **只报告，不自动修** | MC-001 发现的问题只标记和报告，不自动修改内容。所有「该不该做」交给人决策 |
| **SCHEMA 事后涌现** | SCHEMA 不前置定义——从通用模板起步，随每次摄入从原料中自动建议新标签、新模块，经人确认后注册 |

---

## 架构总览

```
┌─────────────────────────────────────────────────────────────┐
│                     摄入管道（实时，同步）                      │
│                                                             │
│  源文 → SOP-001 策展地图生成（自适应提取）                        │
│              ↓                                               │
│         聚类辅助整理                                           │
│              ↓                                               │
│         策展地图（Curation Map） ← 人审的审计面                │
│              ↓                                               │
│         人审（标记：确认入库 / 忽略 / 需要更多源）                │
│              ↓                                               │
│         新 SOP-002 策展入库（待精确批准、事务与回滚协议）          │
│              ↓                                               │
│         SOP-003 格式 Lint（轻量，机械检查）                      │
│                                                             │
│  写入 Agent（摄入会话）和检查 Agent（Cron 会话）上下文隔离       │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│              MC-001 元认知 Agent（每周 Cron，异步）              │
│                                                             │
│  维度 1：遗漏检测（embedding 初筛 + LLM 独立审查）              │
│  维度 2：语义质量抽检（LLM，随机 10% 页面）                     │
│  维度 3：跨页面语义关联发现（embedding，零 LLM 成本）           │
│  维度 4：概念漂移聚合（log.md 扫描）                            │
│  维度 5：结构健康趋势（规则，零 LLM 成本）                      │
│  维度 6：更新知识库健康仪表盘 HTML                              │
│                                                             │
│  输出：趋势报告 + 仪表盘 + 待决策项列表                          │
│  权限：只标记报告，不自动修改内容                                │
│  预算：每周期 LLM 调用有上限                                    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│              Inbox 轻量捕获（即时，异步）                        │
│                                                             │
│  Agent 快速记下随意思考/灵感/备忘 → 本地 Capture Store          │
│              ↓                                               │
│         Global Intake 逻辑视图；可选异步镜像到未审核范围          │
│              ↓                                               │
│         未来的批量审核/分析生成提升提案                          │
│         → 某主题积累 ≥3 条 → 建议提升为正式 wiki 页面            │
│         → 人确认精确变更 → 再进入对应策展流程                    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│              主动检索 + 多知识库路由（会话中）                    │
│                                                             │
│  知识库路由表 → Agent 识别概念 → 自动检索对应 KB                 │
│  主动检索规范 → 概念触发 / 事实确认 / 知识补全                   │
│  跨 KB 联合索引 → MC-001 每周维护                              │
└─────────────────────────────────────────────────────────────┘
```

---

## LLM 的四项根本局限与系统对策

这四个局限是当前 LLM 技术的硬边界，架构不能「解决」它们，只能**降低伤害**、**把不可消除的部分交还给人类**。

| 局限 | 含义 | 架构对策 |
|------|------|---------|
| 无真正语义理解 | LLM 做统计模式匹配，不是推理 | 多轮次提取（分治降低认知负担）+ 提取接口（强迫系统化扫描）+ 聚类辅助（发现 LLM 未标注的关联）+ MC-001 独立交叉验证 |
| 长期连贯思考丢失 | 跨数周复杂推理链丢失上下文 | 知识库作为外部持久记忆 + MC-001 趋势视图 + 概念漂移追踪 + 主动检索注入上下文 |
| 无自主价值判断 | LLM 无法判断「值不值得深挖」 | 人审设在关键决策点 + MC-001 只报告不决策 + 聚类建议只做提示 |
| 无真正创造性连接 | LLM 只在表层做相似度匹配 | MC-001 跨页面/跨 KB 关联发现（AI 做大规模比对）+ 人做创造性判断 + 联合索引降低发现门槛 |
