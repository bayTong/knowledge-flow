# C1 golden fixtures are byte-level contracts and must remain LF on every platform.
/tests/capture/fixtures/*.yaml text eol=lf
/tests/capture/fixtures/*.json text eol=lf

# Preserve archived research inputs byte-for-byte; their Markdown hard breaks are historical data.
/docs/research/2026-09-11/* -text whitespace=-trailing-space
